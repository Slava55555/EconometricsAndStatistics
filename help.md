Для оценки значимости коэффициентов множественной линейной регрессии с помощью **t-критерия Стьюдента** выполните следующие шаги:

### 1. **Модель регрессии**
Модель имеет вид:  
$$ \mathbf{Y} = \mathbf{X}\boldsymbol{\beta} + \boldsymbol{\varepsilon} $$  
где:
- $\mathbf{Y}$ — вектор зависимой переменной (размер $n \times 1$),
- $\mathbf{X}$ — матрица регрессоров (размер $n \times (k+1)$, включая столбец из единиц для свободного члена),
- $\boldsymbol{\beta}$ — вектор коэффициентов (размер $(k+1) \times 1$),
- $\boldsymbol{\varepsilon}$ — вектор ошибок.

### 2. **Оценка коэффициентов**
Оценка коэффициентов методом наименьших квадратов (МНК):  
$$ \hat{\boldsymbol{\beta}} = (\mathbf{X}^T \mathbf{X})^{-1} \mathbf{X}^T \mathbf{Y} $$

### 3. **Остатки и стандартная ошибка регрессии**
- Остатки:  
$$ \mathbf{e} = \mathbf{Y} - \mathbf{X}\hat{\boldsymbol{\beta}} $$  
- Стандартная ошибка регрессии (стандартное отклонение остатков):  
$$ \hat{\sigma} = \sqrt{\frac{\mathbf{e}^T \mathbf{e}}{n - k - 1}} $$

### 4. **Стандартные ошибки коэффициентов**
Ковариационная матрица коэффициентов:  
$$ \text{Cov}(\hat{\boldsymbol{\beta}}) = \hat{\sigma}^2 (\mathbf{X}^T \mathbf{X})^{-1} $$  
Стандартная ошибка коэффициента $\hat{\beta}_j$:  
$$ \text{SE}(\hat{\beta}_j) = \sqrt{\hat{\sigma}^2 \cdot (\mathbf{X}^T \mathbf{X})^{-1}_{jj}} $$  
где $(\mathbf{X}^T \mathbf{X})^{-1}_{jj}$ — $j$-й диагональный элемент матрицы.

### 5. **t-статистика для коэффициентов**
Для проверки гипотезы $H_0: \beta_j = 0$ против $H_1: \beta_j \neq 0$:  
$$ t_j = \frac{\hat{\beta}_j}{\text{SE}(\hat{\beta}_j)} $$  
Эта статистика имеет **t-распределение Стьюдента** с $n - k - 1$ степенями свободы.

### 6. **Проверка значимости**
- Сравните $|t_j|$ с критическим значением $t_{\text{крит}}$ для выбранного уровня значимости $\alpha$ (например, 0.05) и степеней свободы $n - k - 1$.
- Если $|t_j| > t_{\text{крит}}$, коэффициент **статистически значим** (отклоняем $H_0$).

### 7. **p-значение**
Альтернативно, можно вычислить p-значение:  
$$ p_j = 2 \cdot P(T > |t_j|) $$  
где $T$ — t-распределение с $n - k - 1$ степенями свободы.  
Если $p_j < \alpha$, коэффициент значим.

---

### **Пример на Python**
```python
import numpy as np
import statsmodels.api as sm

# Данные: X (матрица регрессоров), Y (вектор зависимой переменной)
X = np.array([[1, x1, x2], ...])  # Добавлен столбец из единиц для константы
Y = np.array([y1, y2, ...])

# Модель регрессии
model = sm.OLS(Y, X).fit()

# Вывод результатов (включая t-статистики и p-значения)
print(model.summary())
```
В выводе `model.summary()` вы увидите:
- `coef` — оценки коэффициентов,
- `std err` — стандартные ошибки,
- `t` — t-статистики,
- `P>|t|` — p-значения.

---

### **Итог**
Если p-значение коэффициента меньше выбранного уровня значимости (например, 0.05), он считается **статистически значимым**.